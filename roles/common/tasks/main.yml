- name: Install dependencies
  import_playbook: ansible-playbook kube-dependencies.yml

- name: Initialize controller
  import_playbook: ansible-playbook controller.yml

- name: Connect worker node to main node
  import_playbook: ansible-playbook workers.yml

- name: Install Argo
  import_playbook: ansible-playbook argo.yml
  when: install_argo == 'true'
